"use client";

import { CustomWorkPreview } from "./CustomWorkPreview";

/**
 * Styles / create surface — only live, usable work (Opening Blessing).
 * Demo theme galleries are not shown.
 */
export function CreateHub() {
  return (
    <main className="bg-[#f8f8fc]">
      <CustomWorkPreview />
    </main>
  );
}
